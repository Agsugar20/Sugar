<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Midnight Writing Studio</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/katex@0.16.0/dist/katex.min.css">
    <script defer src="https://cdn.jsdelivr.net/npm/katex@0.16.0/dist/katex.min.js"></script>
    <script defer src="https://cdn.jsdelivr.net/npm/katex@0.16.0/dist/contrib/auto-render.min.js"></script>

    <style>
        :root { 
            --primary: #f1c40f; /* 별빛 금색 */
            --bg: #0f172a;      /* 깊은 밤의 색 */
            --card-bg: #1e293b; /* 서재 책상 색 */
            --text: #e2e8f0; 
        }
        body { 
            font-family: 'Pretendard', 'Apple SD Gothic Neo', serif; 
            background: var(--bg); 
            color: var(--text);
            display: flex; justify-content: center; padding: 40px 20px; 
            margin: 0;
        }
        .container { width: 100%; max-width: 800px; }
        .card { 
            background: var(--card-bg); padding: 30px; border-radius: 20px; 
            box-shadow: 0 10px 30px rgba(0,0,0,0.5); margin-bottom: 25px;
            border: 1px solid rgba(255,255,255,0.05);
        }
        h2 { margin-top: 0; font-size: 1.2rem; color: var(--primary); letter-spacing: 1px; }
        
        .input-group { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; margin-bottom: 10px; }
        .input-box { display: flex; flex-direction: column; }
        label { font-size: 0.8rem; margin-bottom: 8px; opacity: 0.8; }
        input { 
            padding: 12px; border: 1px solid #334155; border-radius: 8px; 
            font-size: 1rem; background: #0f172a; color: white; outline: none;
        }
        input:focus { border-color: var(--primary); }
        
        .progress-wrapper { margin: 30px 0; }
        .bar-bg { background: #334155; height: 8px; border-radius: 10px; overflow: hidden; }
        .bar-fill { 
            background: linear-gradient(90deg, #f1c40f, #e67e22); 
            height: 100%; width: 0%; transition: 1s cubic-bezier(0.22, 1, 0.36, 1); 
            box-shadow: 0 0 10px rgba(241, 196, 15, 0.5);
        }
        
        #percentageText { font-family: 'Georgia', serif; font-size: 2rem; margin-bottom: 5px; }
        .formula-display { 
            background: rgba(15, 23, 42, 0.6); padding: 25px; border-radius: 15px; 
            font-size: 1.1rem; overflow-x: auto; text-align: center;
            border: 1px dashed rgba(241, 196, 15, 0.3);
        }
    </style>
</head>
<body>

<div class="container">
    <div class="card">
        <h2>🌙 NIGHT WRITING STUDIO</h2>
        <div class="input-group">
            <div class="input-box">
                <label>목표량 (Goal)</label>
                <input type="number" id="targetWords" value="50000" oninput="updateAll()">
            </div>
            <div class="input-box">
                <label>현재 달성 (Current)</label>
                <input type="number" id="currentWords" value="15200" oninput="updateAll()">
            </div>
        </div>
    </div>

    <div class="card">
        <h2>📊 PROGRESS ANALYSIS</h2>
        <div id="percentageText">0%</div>
        <div class="progress-wrapper">
            <div class="bar-bg"><div id="progressBar" class="bar-fill"></div></div>
        </div>
        
        <div id="mathDisplay" class="formula-display"></div>
    </div>
</div>

<script>
    function updateAll() {
        const target = document.getElementById('targetWords').value || 1;
        const current = document.getElementById('currentWords').value || 0;
        const percent = ((current / target) * 100).toFixed(1);
        const remaining = target - current;

        document.getElementById('progressBar').style.width = percent + "%";
        document.getElementById('percentageText').innerText = `${percent}%`;

        const mathArea = document.getElementById('mathDisplay');
        const formula = `
            \\text{Efficiency: } \\eta = \\frac{${current}}{${target}} \\approx ${percent}\\% \\\\
            \\text{To go: } W_{remain} = ${remaining.toLocaleString()}
        `;
        
        mathArea.innerHTML = "$$" + formula + "$$";
        
        renderMathInElement(document.body, {
            delimiters: [{left: "$$", right: "$$", display: true}]
        });

        localStorage.setItem('writingData_midnight', JSON.stringify({target, current}));
